import { Meta } from "@storybook/addon-docs";
import * as stories from "./default.stories.js";

<Meta title="Components 🧩/👷‍♂️ Templates/📱 Extension" />

# Layout
## Default (extension)
The below is the `Default` template that wraps all pages of the extension application.

<Canvas>
  <Story story={stories.TemplatePageLayoutDefault} name="Default"/>
</Canvas>
