import { Meta } from "@storybook/addon-docs";
import * as stories from "./app-shell.stories.js";

<Meta title="Components 🧩/👷‍♂️ Templates/📱 Extension" />

# Layout
## App Shell (extension)
The below is the `App Shell` template that wraps all pages of the extension application.

<Canvas>
  <Story story={stories.TemplatePageLayoutAppShell} name="App Shell"/>
</Canvas>
